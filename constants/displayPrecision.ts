// constants/displayPrecision.ts
// Shared constants for display precision across widgets

/**
 * Default percentage precision for home screen display
 * Used by CurrentVolatilityWidget and CurrentDominanceWidget on the home screen
 */
export const HOME_SCREEN_PERCENTAGE_PRECISION = 1;

/**
 * Default percentage precision for other screens/contexts
 * Can be overridden per widget instance if needed
 */
export const DEFAULT_PERCENTAGE_PRECISION = 2;

